 <script src="https://cdn.jsdelivr.net/npm/@tailwindcss/browser@4"></script>
<%- include('partial/nav')%>

<!-- Introduction Section -->
<section class="flex flex-col-reverse lg:flex-row justify-center items-center gap-8 p-6 lg:p-16 h-159">
  <!-- Left Section: Text + Social + CV -->
  <div class="text-center lg:text-left lg:w-1/2">
    <h1 class="text-2xl font-semibold">Hello, it's Me</h1>
    <h2 class="text-4xl font-bold mb-2">Dharmavir Singh  </h2>
    <h3 class="text-4xl font-bold mb-2">I`am  </h3>
    <style>
    
    .cursor {
      display: inline-block;
      margin-left: 2px;
      width: 10px;
      height: 1em;
      background-color: #00ff99;
      animation: blink 0.7s infinite;
    }
    @keyframes blink {
      0%, 100% { opacity: 1; }
      50% { opacity: 0; }
    }
  </style>


  <div>
    <span id="typewriter"></span><span class="cursor"></span>
  </div>

  <script>
    const texts = [
      "Web Developer 🚀",
      "Full Stack Engineer 💻",
      "Creative Coder 🎨",
      "Let's Build Something Awesome!"
    ];

    let index = 0;        // text index
    let charIndex = 0;    // character index
    let typing = true;    // typing or deleting
    const speed = 100;    // typing speed
    const delay = 1000;   // delay between texts
    const element = document.getElementById("typewriter");

    function typeLoop() {
      const currentText = texts[index];

      if (typing) {
        element.textContent = currentText.substring(0, charIndex++);
        if (charIndex > currentText.length) {
          typing = false;
          setTimeout(typeLoop, delay); // pause before deleting
          return;
        }
      } else {
        element.textContent = currentText.substring(0, charIndex--);
        if (charIndex < 0) {
          typing = true;
          index = (index + 1) % texts.length;
        }
      }

      setTimeout(typeLoop, speed);
    }

    typeLoop();
  </script>



    <div class="flex justify-center lg:justify-start gap-4 mt-4">
      <img class="w-10 rounded-full border-2 border-amber-600" src="/img/linkedin.png" alt="linkedin">
      <img class="w-10 rounded-full border-2 border-amber-600" src="/img/facebook.png" alt="facebook">
      <img class="w-10 rounded-full border-2 border-amber-600" src="/img/instagram.png" alt="instagram">
      <img class="w-10 rounded-full border-2 border-amber-600" src="/img/twitter.png" alt="twitter">
    </div>

    <div class="mt-6">
      <a href="/img/mycv.pdf" download>
        <button class="border border-amber-600 px-6 py-2 bg-amber-600 rounded hover:text-amber-100 text-white font-semibold hover:bg-white hover:text-amber-600">Download CV</button>
      </a>
    </div>
  </div>

  <!-- Right Section: Image -->
  <div class="lg:w-1/3">
    <img class="w-72 border-b-4 border-amber-600" src="/img/Screenshot 2025-04-09 at 10.45.44 PM (1).png" alt="Dharmavir Singh">
  </div>
</section>



<%- include('partial/footer')%>